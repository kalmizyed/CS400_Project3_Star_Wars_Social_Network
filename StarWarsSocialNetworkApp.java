/**
 * Loads the data and starts the app.
 */
public class StarWarsSocialNetworkApp {
    /**
     * Starts the app.
     */
    public static void main(String[] args) {
        IGraphBuilder builder; // = new GraphBuilder(); TODO Data Wrangler
        ExtendedGraphADT<String> graph; // = builder.getGraph(); TODO Algorithm Engineer
        IStarWarsSocialNetworkBackend backend; // = new StarWarsSocialNetworkBackend(graph);
        IStarWarsSocialNetworkFrontend frontend; // = new StarWarsSocialNetworkFrontend(backend); TODO Frontend Developer
        // TODO run Frontend
    }
}
